import React, { Component } from 'react';

import styles from './GlobalDeaths.module.css';

export default class GlobalDeaths extends Component {
    render() {
        return(
         <div className={styles.globalDeaths}>GlobalDeaths</div>
        )
      }
}