import React, { Component } from 'react';

import styles from './CovidGraph.module.scss';

export default class CovidGraph extends Component {
    render() {
        return(
         <div className={styles.covidGraph}>CovidGraph</div>
        )
      }
}