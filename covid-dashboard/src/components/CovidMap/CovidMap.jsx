import React, { Component } from 'react';

import styles from './CovidMap.module.css';

export default class CovidMap extends Component {
    render() {
        return(
         <div className={styles.covidMap}>CovidMap</div>
        )
      }
}